# Accessibility Rules

- Components must pass AXE checks and meet WCAG AA contrast.
- Provide keyboard support for interactive components.
- Use semantic HTML and ARIA attributes where appropriate.
- Ensure focus-visible states and focus management are implemented.
- Avoid conveying intent using color alone.