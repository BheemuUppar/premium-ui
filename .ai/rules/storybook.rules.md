# Storybook Rules

- Use modern Storybook Angular syntax with typed `Meta` and `StoryObj`.
- Use `render` functions and `props` for interactive stories.
- Keep stories visual and accessible; include keyboard interactions when relevant.
- Do not include implementation secrets or heavy logic inside stories.
- Prefer standalone component imports in stories.