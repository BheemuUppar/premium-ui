# Token Rules

- Use existing tokens under `src/premium-ui/tokens`.
- Do not hardcode colors, spacing, or radii; reference `--pui-` variables.
- Add new tokens only when necessary and with a matching design rationale.
- Prefix CSS variables with `--pui-` and maintain semantic names (e.g., `--pui-card-bg`).