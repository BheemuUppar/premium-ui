# Card Pattern

- Variants: default, outlined, elevated, ghost.
- Sizes: sm, md, lg.
- Inputs: `variant`, `size`, `hoverable`, `interactive`, `disabled`.
- Slots: header, content, footer using `ng-content select`.
- Themed via tokens: background, border, shadow, radius.