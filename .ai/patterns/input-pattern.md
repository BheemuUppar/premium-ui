# Input Pattern

- Use native input elements with proper ARIA roles.
- Use `NgOptimizedImage` only for images (not base64).
- Expose `value`, `disabled`, `readonly`, and `ariaLabel` inputs.
- Provide clear focus-visible state and error messaging.