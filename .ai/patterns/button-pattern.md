# Button Pattern

- `pui-button` with variants: primary, secondary, ghost, destructive.
- Expose `type`, `disabled`, `ariaLabel`, and `loading`.
- Use CSS tokens for color, radius, and motion.
- Support icon slot via `<ng-content select="[icon]">`.