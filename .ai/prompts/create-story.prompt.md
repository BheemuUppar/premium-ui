# Create Story Prompt

When creating Storybook stories:
- Provide `Meta` with typed component and `tags: ['autodocs']`.
- Add controls for variant/size/disabled/etc.
- Include render functions with `props` and `template`.
- Add examples for variants, sizes, and interactive states.
- Ensure stories compile in Angular Storybook.