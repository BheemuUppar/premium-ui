import type { PuiSize } from '../../types/common.types';

export type PuiButtonVariant = 'primary' | 'secondary' | 'outline' | 'ghost' | 'danger';

export type PuiButtonSize = PuiSize;

export type PuiButtonType = 'button' | 'submit' | 'reset';
