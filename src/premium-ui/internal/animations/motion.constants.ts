/** Shared animation token references for Premium UI. */
export const PUI_MOTION = {
  durationFast: 'var(--pui-duration-fast)',
  durationNormal: 'var(--pui-duration-normal)',
  durationSlow: 'var(--pui-duration-slow)',
  easingStandard: 'var(--pui-easing-standard)',
  easingPremium: 'var(--pui-easing-premium)',
} as const;
