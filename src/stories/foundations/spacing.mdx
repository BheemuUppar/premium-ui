import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Foundations/Spacing" />

# Spacing

Spacing follows an 8px-based scale with compact steps for fine UI control.

<div style={{ display: 'grid', gap: '0.75rem' }}>
  {['xxs', 'xs', 'sm', 'md', 'lg', 'xl', '2xl', '3xl'].map((size) => (
    <div key={size} style={{ display: 'grid', gridTemplateColumns: '5rem 1fr', alignItems: 'center', gap: '1rem', fontFamily: 'var(--pui-font-family)' }}>
      <code>{size}</code>
      <span style={{ display: 'block', width: `var(--pui-space-${size})`, height: '1rem', borderRadius: 'var(--pui-radius-sm)', background: 'var(--pui-color-primary)' }} />
    </div>
  ))}
</div>
