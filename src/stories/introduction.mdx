import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Introduction" />

# Premium UI

Premium UI is an Angular component foundation built around standalone components, CSS custom property tokens, SCSS, accessibility-first defaults, and tree-shakable exports.

Use the toolbar theme switcher to preview components in light and dark themes.
